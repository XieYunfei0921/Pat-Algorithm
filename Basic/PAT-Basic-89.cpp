#include <iostream>
#include <algorithm>
using namespace std;
int main(int argc, char const *argv[])
{
    int n;
    cin>>n;
    int info[n];
    for (int i = 0; i < n; i++)
        cin>>info[i];
    sort(info,info+n);
    int size=n;
    for (int i = 0; i < n; i++)
    {
        if()   
    }
    return 0;
}
